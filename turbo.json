{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["CONVEX_DEPLOYMENT", "CONVEX_DEPLOY_KEY", "VITE_CONVEX_URL"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "^typecheck"],
      "outputs": ["build/**", "dist/**"]
    },
    "codegen": {
      "outputs": ["_generated/**", "client/hook-api.tsx"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^test"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": []
    },
    "typegen": {
      "outputs": [".react-router/types/**"]
    }
  }
}
